#specify piss

func main(){
  string kok = "hello world"
  int a = 1
  int g = 10
  int u = .5+a

  c.typedown(g+a+1)
  c.typedown(kok)
  c.typedown(a)
  c.typedown(g)
  c.typedown(u)
  c.typedown(g-u+10/g)
  c.wait(3)
  c.typedown(&"hello world")
  c.typedown(-40*1.8+32)
  c.typedown()
  c.intmemory()
  c.strmemory()
}